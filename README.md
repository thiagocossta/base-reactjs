# Estrutura base para desenvolvimento em ReactJs

## Features

Eslint (modelo airbnb) e Prettier configurados.

## Instruções

- clone o repositório
- execute `npm install` ou `yarn install`
- execute `yarn start`
- divirta-se
